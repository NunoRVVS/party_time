<h1>Order</h1>
<ul>
  <% @orders.each do |order| %>
  <li><%= link_to "Order ##{order.id}", order_path(order) %> :: <%= order.party_item.name %> starting on: <%= order.start %> </li>

  <% end %>
</ul>
