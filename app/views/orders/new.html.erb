<h1>New Order</h1>
<%= simple_form_for [@party_item, @order] do |f| %>
  <%= f.input :start_date, as: :date, min: Date.today, html5: true %>
  <%= f.input :end_date, as: :date, min: Date.tomorrow, html5: true %>
  <%= f.submit "Submit Order", class: "btn btn-primary" %>
<% end %>
